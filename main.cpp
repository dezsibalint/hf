#include <iostream>
#include "aramkorielem.h"
#include "forras.h"
#include "vezetek.h"
#include "norgate.h"
#include "invereter.h"
#include "kombhal.h"
#include "memtrace.h"
#include "halozat.h"
int main() {

   /* v1.connect(f1.out());
    v2.connect(f2.out());
    norgate n(5);
    n.connect(v1.out(),0);
    n.connect(v2.out(),1);
    n.print();
    invereter i;
    i.connect(n.out(),0);
*/
   kombhal a;
   a.start();


}
